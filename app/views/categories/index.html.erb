<h1>Categories</h1>

<%= form_for :category do |c| %>
<%= c.text_field :name, placeholder: "name your category" %>

<%= c.submit 'make category', class: 'btn btn-default'%>


<br/>

<% @categories.each do |c| %>

<%= c.name %>
<% end %>
