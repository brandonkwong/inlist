<!-- Modals -->
<%= render 'partials/item_user' %>

<!-- Tag Query Results -->
<%= render 'partials/tag_query' %>

<!-- Collection -->
<div data-ng-if="tagQuery.name == ''">
<h2>InList / <%= @user.username %></h2>
<br>
<!-- Collection -->
<% @user.items.each do |i| %>

    <!-- Collection Row -->
    <% if @user.items.index(i) == 0 || (@user.items.index(i)+1) % 4 == 0 %>
       <div class="row">
    <% end %>

    <!-- Items -->
    <div class="col-md-3">
        <div data-toggle="modal" data-target="#item-<%= i.id %>">
            <div class="item-box">
                <%= image_tag i.image, class: 'item-image' %>
            </div>
        </div>
    </div>

    <!-- Collection Row End -->
    <% if (@user.items.index(i)+1) % 4 == 0 %>
        </div>
    <% end %>

<% end %>
</div>
